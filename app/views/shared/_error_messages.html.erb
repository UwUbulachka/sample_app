<% if object.errors.any? %> <!-- если у пользователя не пустая ошибка (есть ошбка) -->
  <div id="error_explanation">
    <div class="alert alert-danger">
     The form contains <%= pluralize(object.errors.count, "error") %>. <!-- возврощай кол-во ошибок -->
    </div>
    <ul>
	<% object.errors.full_messages.each do |msg| %> <!-- и перечисли все ошибки -->
	  <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>  