<% if @feed_items.any? %> <!-- если не пустой то -->
  <ol class="microposts">
      <%= render @feed_items %> <!-- верни посты app/views/microposts/_micropost.html.erb -->
  </ol>
  <%= will_paginate @feed_items %>    <!-- и разбей из -->
<% end %>