<% unless current_user?(@user) %>
 <div id="follow_form">
    <% if current_user.following?(@user) %> <!-- если текущий пользователь подписан на пользователя -->
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>